spec_version: "0.1.0"
name: whyAI
version: 0.1.0
description: Gitagent agent for whyAI
model:
  preferred: "openai:gpt-4o-mini"
  fallback: []
tools: [cli, read, write, memory]
runtime:
  max_turns: 50
